# Anthropic
ANTHROPIC_API_KEY=

# Notion
NOTION_API_KEY=
# Optional: Notion database ID to search first on every query (falls back to general search if no results)
NOTION_PRIORITY_DB_ID=

# Slack (xoxp- user token — requires search:read scope)
SLACK_USER_TOKEN=
# Optional: comma-separated channel names to restrict search (e.g. general,cs-team,higherme-internal)
SLACK_CHANNEL_WHITELIST=

# Zoom Server-to-Server OAuth app
ZOOM_ACCOUNT_ID=
ZOOM_CLIENT_ID=
ZOOM_CLIENT_SECRET=
